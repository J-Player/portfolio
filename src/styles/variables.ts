// eslint-disable-no-duplicate-enum-values

export enum BackgroundColor {
	PRIMARY = '#000025',
	SECONDARY = '#ffffff',
	TERNARY = '#EBEBEB'
}

export enum FontColor {
	PRIMARY = '#ffffff',
	SECONDARY = '#000025',
	HIGHLIGHT = '#00FF80'
}

export const Color = {
	backgroundColor: BackgroundColor,
	fontColor: FontColor
}
